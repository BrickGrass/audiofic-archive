uuid: 0b2eeb50-8841-4d47-be26-36d5b9b746e2
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_reader
    - node.type.work
    - taxonomy.vocabulary.reader
id: node.work.field_reader
field_name: field_reader
entity_type: node
bundle: work
label: Podficcer
description: 'Add the list of readers who should be credited on the work. Credited readers can remove themselves. Only those readers who are also listed in the owner field will be able to edit or delete the work. If there are other non-podficcer contributors (e.g. editors, coordinators of multivoice works) you can mention them in the Notes field.'
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      reader: reader
    sort:
      field: name
      direction: asc
    auto_create: true
    auto_create_bundle: ''
field_type: entity_reference
