uuid: d0a5be20-bf5a-4dcc-afa0-621b59d76182
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_author
    - node.type.work
    - taxonomy.vocabulary.author
id: node.work.field_author
field_name: field_author
entity_type: node
bundle: work
label: Author
description: "Always use the drop-down version if it's already in the archive to avoid creating duplicates. "
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      author: author
    sort:
      field: name
      direction: asc
    auto_create: true
    auto_create_bundle: ''
field_type: entity_reference
