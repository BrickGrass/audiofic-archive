uuid: 365563a0-cb8d-4b31-8a09-497a10df5d27
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_cover
    - node.type.work
  module:
    - image
id: node.work.field_cover
field_name: field_cover
entity_type: node
bundle: work
label: Cover
description: "Having a cover is optional. Ideally, use a square image. The system will crop images that aren't square so if your text runs right across a rectangular file, parts will be cropped off. However, this is as our display style for images is square - your image is stored in its original shape, so you can still use the link to display it in full elsewhere. "
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:file'
  handler_settings: {  }
  file_directory: covers
  file_extensions: 'png gif jpg jpeg'
  max_filesize: '20 MB'
  max_resolution: 1600x1600
  min_resolution: 300x300
  alt_field: true
  alt_field_required: true
  title_field: false
  title_field_required: false
  default_image:
    uuid: ''
    alt: ''
    title: ''
    width: 500
    height: 500
field_type: image
