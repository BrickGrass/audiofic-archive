name: Propagate Metadata
description: A custom module for the audiofic archive which enforces & calculates fields for users, works and series.
core_version_requirement: ^11
type: module
dependencies:
  - aa_utils:aa_utils